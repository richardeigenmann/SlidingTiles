language: cpp
dist: trusty
sudo: false

compiler:
  - gcc
  - clang

env:
    COMPILER=gcc-4.9

addons:
  apt:
    packages:
    - libsfml-dev
    - lcov
    - gcc-4.9

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - make test
