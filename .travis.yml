language: cpp
dist: trusty
sudo: false

compiler:
  - gcc
  - clang

env:
    COMPILER=g++-6

addons:
  apt:
    packages:
    - libsfml-dev
    - lcov
    - g++-6

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - make test
